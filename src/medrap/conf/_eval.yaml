defaults:
  - encoder: meds_code
  - query_projector: identity
  - retriever: static
  - retrieval_encoder: identity
  - fusion: replace
  - pooling: identity
  - head: linear
  - lightning_module: default
  - datamodule: default
  - trainer: default
  - _self_

output_dir: .
run_smoke: false
checkpoint_path: null
eval_split: test

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
