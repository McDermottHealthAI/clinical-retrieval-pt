defaults:
  - encoder: meds_code
  - query_projector: sequence_mean
  - retriever: in_memory_topk
  - retrieval_encoder: mean_pooled
  - fusion: replace
  - pooling: identity
  - head: linear
  - _self_

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
