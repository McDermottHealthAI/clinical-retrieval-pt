defaults:
  - model: default
  - task: binary
  - metrics: binary
  - batch_adapter: default
  - optimizer: adamw
  - LR_scheduler: none
  - lightning_module: default
  - datamodule: meds_torchdata
  - callbacks: default
  - logger: csv
  - trainer: default
  - _self_

max_seq_len: 256
output_dir: .
run_smoke: false
resume_from_checkpoint: null
run_test_after_fit: false
test_ckpt_path: null

hydra:
  run:
    dir: .
  output_subdir: null
  job:
    chdir: false
